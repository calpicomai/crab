# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments (one per node — see README)
.venv/
robot/.venv/
brain/.venv/
venv/
env/

# Local config / secrets (protocol config is in code; anything local goes here)
.env
*.local
crab.env          # per-rig launcher prefs written by brain/run.sh
.run/             # launcher runtime logs (perception/sim server)

# Editor / OS
.vscode/
.idea/
.DS_Store
